DOWNLOAD_DOMAIN=http://localhost:8000
ENV_TYPE=dev
PORT=8000
# UPLOAD_DIR_NAME defaults to "storage", no need to set typically
MAX_FILE_SIZE_MB=10
LINK_EXPIRY_MINUTES=60
CLEANUP_INTERVAL_SECONDS=300
# ALLOWED_MIME_TYPES_REGEX defaults to "png|jpeg|jpg|gif|webp|tiff"
# LOG_LEVEL defaults to "info"
